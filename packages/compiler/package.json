{
  "name": "@workflow-extractor/compiler",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "bin": {
    "workflow-extractor": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "dev": "node scripts/serve-demo.mjs",
    "dev:cli": "tsc -p tsconfig.build.json --watch",
    "lint": "eslint src --ext .ts",
    "test": "vitest run"
  },
  "dependencies": {
    "commander": "^12.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tslib": "^2.8.1",
    "vitest": "^2.1.8"
  }
}
